hasher.ps1 -- Unused, generates hash for msi and exe.

hasher.sh -- Unused, generates hash for appimage.

install.nsi -- NSIS (EXE) Installer script.

install.wxs -- CMake configure-file template for WIX (MSI) installer. 

linux_release.sh -- Build, then package appimage.

makeappimage.sh -- Package appimage.

makemsi.cmd -- Used by cmake, do not directly execute -- Packages wix installer.

makensi.cmd -- Packages NSIS installer from build at ../build

win32_release.ps1 -- Build, then package wix (MSI) installer, generate doxygen, package assets + docs into nsis (EXE) installer.
